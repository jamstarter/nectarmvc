tables:
  userauth_users:
    primary_key: id
    columns:
      id:
        type: int(11)
        extra: auto_increment
      email: 
        type: varchar(255)
      password:
        type: varchar(255)
      first_name:
        type: varchar(255)
      last_name:
        type: varchar(255)
      date_created:
        type: timestamp
        default: current_timestamp
